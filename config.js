// Globale Konfiguration für freie, programmatische KI-Knoten (ähnlich wie Blockchain RPCs)
export const CONFIG = {
    // Freie Community-Endpunkte für Open-Source LLMs (Llama-3 / Phi-3)
    GLOBAL_ENDPOINTS: [
        "https://huggingface.co",
        "https://huggingface.co"
    ],
    // Fallback falls ein Knoten überlastet ist
    DEFAULT_THRESHOLD: 0.35
};
